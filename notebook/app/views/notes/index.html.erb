<h1>Your notes</h1>
 
<table>
  <tr>
    <th></th>
  </tr>
 
  <% @note.each do |note| %>
    <tr>
      <td><%= note.title %></td>
      <td><%= link_to 'Show', note_path(note) %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New', new_note_path(@note)%>